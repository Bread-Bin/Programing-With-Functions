import math
import time
import pytest

"global varable is not a good practice. "
"try to keep varible within a function like Def"
" and other blocks of code"

def subject():
    subject = "what ever varibles you want"
    print(subject)

subject() # calling the function.

def main():


    while True:
        Value = input("Enter a number: ")
        try:
            return float(Value)
        except ValueError:
            print("Invalid input. Please enter a number.")

main()

for i in range(5):
    print(i)

for i, fruit in enumerate(["apple", "banana", "cherry"]):
    print(i, fruit)

uppered = [fruit.upper() for fruit in fruit]
print(uppered)


grades = {"Alice": 85, "Bob": 92, "Charlie": 78}
print("All grades:", grades)

print()
